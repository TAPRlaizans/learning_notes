#include <iostream>
#include <stdio.h>
// #include <stdlib.h>
 
#define MAX(x,y) (((x)>(y))?(x):(y))
#define MIN(x,y) (((x)<(y))?(x):(y))

int main()
{
    void *p = malloc(10);
	system("pause");
	return 0;
}