#pragma once 
#include "glog/logging.h"
#include <string>

class Log
{
public:
    Log();  
    ~Log();
    bool test();
    // bool init();
    // bool log(std::string level, std::string msg);
    // bool close();
};